function setup() {
    createCanvas(400, 400);
    noLoop();
    background(204);
}

function draw() {
    for (let j=0; j<6; j++) {
	for (let i=0; i<8; i++) {
	    ellipse(i*30, j*30, 20, 20);
	}
   }
}
